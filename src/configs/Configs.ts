export type Configs = {
  port: number;
  nodeEnv: string;
  routerPathV1: string;
  routerPathV2: string;

  mysqlHost: string;
  mysqlPort: number;
  mysqlUser: string;
  mysqlPassword: string;
  mysqlUrl: string;

  paginationLimit: number;
};
